export const kelvinToCelsius = (temp) => (temp - 273.15).toFixed(2);  
export const kelvinToFahrenheit = (temp) => ((temp - 273.15) * 9/5 + 32).toFixed(2);
export const kelvinToKelvin = (temp) => temp.toFixed(2);  // Keep in Kelvin
